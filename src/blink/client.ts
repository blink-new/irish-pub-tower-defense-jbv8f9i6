import { createClient } from '@blinkdotnew/sdk';

export const blink = createClient({
  projectId: 'irish-pub-tower-defense-jbv8f9i6',
  authRequired: true // Enable authentication for AI features
});